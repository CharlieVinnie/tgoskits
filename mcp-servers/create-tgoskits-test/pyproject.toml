[project]
name = "create-tgoskits-test"
version = "0.1.0"
description = "MCP server to scaffold new tgoskits StarryOS test cases"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.27.0",
]
